import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff'
  },
  tabBarIcon: {
    width: 26,
    height: 26
  }
});

export default styles
