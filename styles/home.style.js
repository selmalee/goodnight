import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%'
  },
  iconStats: {
    position:'absolute',
    bottom: 10,
    right: 10
  }
});

export default styles
